.venv/
.venv-rvc/
__pycache__/
*.pyc
rvc_models/
config/settings.json
*.log
.claude/
data/
*.db

# Skill generate da Jake (specifiche di questo PC)
plugins/learned_*.py

# HUD nativo (v4.9.x): output di build C++/CMake, rigenerabile con cmake --build
hud/native/build/
hud/native/build2/
# Se uno strumento (es. estensione CMake Tools) configura dalla radice invece che da
# hud/native/, l'output finisce qui per errore: va ignorato comunque, mai committato
# (successo il 2026-09-08, vedi CMakePresets.json che fissa binaryDir).
/build/

# F0: risultati dei benchmark (benchmarks/*.py) - numeri specifici di QUESTA macchina in un
# momento preciso, non fanno parte del codice versionato.
benchmarks/results/

# Diagnostica prodotta localmente dallo stesso step usato in CI; il runner la pubblica come
# artifact, ma non deve mai entrare nel repository.
.ci-artifacts/
