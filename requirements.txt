# F0 (roadmap): le dipendenze sono ora divise per area in requirements/ (base, voce, GUI legacy,
# HUD, GPU, sviluppo) cosi' un'installazione parziale (es. CI senza voce/HUD) puo' scegliere solo
# i gruppi che le servono. Questo file resta il punto d'ingresso usato da setup.ps1 per
# un'installazione completa: aggrega tutto tranne requirements/gpu.txt (opzionale, solo con GPU
# NVIDIA, installato a parte da setup.ps1) e requirements/dev.txt (solo sviluppo/CI).
-r requirements/base.txt
-r requirements/voice.txt
-r requirements/gui.txt
-r requirements/hud.txt
